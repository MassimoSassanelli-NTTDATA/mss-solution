<Project>
  <PropertyGroup>
    <!-- When true the solution will use project references, otherwise it will use NuGet packages -->
    <UseLocalProjects>false</UseLocalProjects>
  </PropertyGroup>
</Project>